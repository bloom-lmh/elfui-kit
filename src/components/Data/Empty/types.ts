export interface EmptyProps {
  image: string;
  imageSize: number | string;
  description: string;
}

export interface EmptySlots {
  default?: unknown;
  image?: unknown;
  description?: unknown;
}
