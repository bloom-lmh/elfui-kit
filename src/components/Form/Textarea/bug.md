# Textarea 多行输入框组件 Bug 记录与修改情况

| Bug 描述                                                                  | 根本原因分析                                                                                                                                                            | 修复方案与改动细节                                                                                | 状态       |
| :------------------------------------------------------------------------ | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------ | :--------- | ----------------------------------------------------------------------------- | ---------- |
| 高度自适应计算（autosize）在初次渲染时可能因取不到 css 样式导致行高为 NaN | 直接使用 `parseInt(getComputedStyle(ta).lineHeight)` 时如果 CSS 样式尚未完成注入（例如在 Custom Element 初次连接阶段），可能得到字符串 `"normal"`，导致转换为数值失败。 | 增加兜底逻辑 `parseInt(getComputedStyle(ta).lineHeight, 10)                                       |            | 20`，保证无论何种情形下行高计算均有默认安全值，杜绝自适应高度计算崩溃的问题。 | **已修复** |
| 输入字符达到 maxLength 后无视觉警告                                       | 原生 maxLength 只会阻断输入但没有视觉字数越界警示。                                                                                                                     | 精准展示字数统计 `countText()`，在超出时在 `wrapper` 及 `count` 节点中搭配 CSS 反射红色警告高亮。 | **已修复** |
