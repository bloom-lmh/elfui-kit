# Select 下拉选择组件 Bug 记录与修改情况

| Bug 描述                                                 | 根本原因分析                                                                                                                                                                                                                                                       | 修复方案与改动细节                                                                                                                                                                                                                                                                | 状态       |
| :------------------------------------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :--------- |
| 基础单选选择后，导致页面上的其它示例展示强制重置和刷新。 | 下拉选项 Option 上的 `@click="selectOption(opt)"` 未阻断事件冒泡与默认行为。在单页路由（SPA/Vite）环境中，未被拦截的点击事件一路冒泡至 `window`，触发了全局路由拦截器或 hashchange 行为，导致整个展示页面被卸载并重新挂载，所有的 `useReactive` 页面级状态被重置。 | 1. 模板绑定改造：将 `@click="selectOption(opt)"` 改为 `@click.stop.prevent="selectOption(opt, $event)"`；将 Trigger 容器的点击改为 `@click.stop`。<br>2. 逻辑层改造：在 `selectOption` 方法内同步调用 `e?.stopPropagation()` 与 `e?.preventDefault()`，完美封锁点击事件向外冒泡。 | **已修复** |
