# FormItem 表单项组件 Bug 记录与修改情况

| Bug 描述                                                 | 根本原因分析                                                                                      | 修复方案与改动细节                                                                                                                                         | 状态       |
| :------------------------------------------------------- | :------------------------------------------------------------------------------------------------ | :--------------------------------------------------------------------------------------------------------------------------------------------------------- | :--------- |
| 组件动态注销后，表单项仍留在 Form 校验列表中导致校验出错 | 宿主组件动态切换表单结构（例如通过 `v-if`），FormItem 在 unmount 时没有从父 Form 容器中彻底注销。 | 在 `onBeforeUnmount` 钩子中，显式调用由 `registerItem` 返回的注销闭包 `unreg()` 并再次调用 `form.unregisterItem(itemCtx)` 做双重保险，彻底移除表单项实例。 | **已修复** |
| 无法通过 `prop="user.name"` 校验嵌套多级对象字段         | 直接使用 `form.model[props.prop]` 读取只能处理一级扁平对象属性。                                  | 封装 `getPath` 与 `setPath` 工具函数，利用递归或循环机制按路径（如 `a.b.c`）寻址深度对象字段。                                                             | **已修复** |
