# Checkbox 复选框组件 Bug 记录与修改情况

| Bug 描述                                                                     | 根本原因分析                                                                                                                                                                                                                                                                                                                                | 修复方案与改动细节                                                                                                                                                                                                                                                                                                                                                       | 状态       |
| :--------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :--------- |
| 单个 Checkbox 选择不了，且点击后会导致其它 Showcase 页面示例强制重置与刷新。 | 1. **注入失效**：框架底层 `createInjectionKey` 原先使用 `Symbol("description")`，在 Vite showcase 局部多实例打包中，各实例持有的 Symbol 不相等，导致 `inject(CHECKBOX_GROUP_KEY)` 失效返回 `undefined`，无法在 Group 下正常工作。<br>2. **冒泡刷新**：点击事件未在最底层被完全拦截和 prevent，冒泡至全局 window 路由层，迫使 SPA 全局重置。 | 1. **底层修复**：将底座 `inject.ts` 的 `PROVIDES_KEY`、`INSTANCE_KEY` 以及 `createInjectionKey` 重构为基于 `Symbol.for` 全局共享注册表的实现，完美解决上下文丢失问题。<br>2. **阻断改造**：在 `Checkbox` 模板子节点 `.box` 与 `.label` 上同时增设 `@click.stop.prevent="toggle"` 拦截保护，并在 `toggle(e)` 中严格执行 `e?.stopPropagation()` 与 `e?.preventDefault()`。 | **已修复** |
