# Space 间距

`elf-space` 为一组兄弟元素提供一致的水平或垂直间距，不搬运或包裹用户 Light DOM。

```html
<elf-space size="default" wrap>
  <elf-button>保存</elf-button>
  <elf-button>取消</elf-button>
</elf-space>
```

核心属性：`direction`、`alignment`、`size`、`spacer`、`wrap`、`fill`、`fill-ratio`。
