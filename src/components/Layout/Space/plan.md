# Space Element Plus API 对标计划

- [x] 支持 `horizontal` / `vertical` 方向和 `alignment` 对齐。
- [x] 支持 small/default/large、CSS 长度、数值和二维元组间距。
- [x] 支持 wrap、fill、fill-ratio 和字符 spacer，不搬运用户 Light DOM。
- [x] 补齐类型、注册、单测、页面案例、API 表格和中英文文案。
- [x] 真实浏览器验证字符 spacer、响应式换行、明暗主题和无横向溢出。
- [ ] 全量测试、生产构建和组件库构建通过。
