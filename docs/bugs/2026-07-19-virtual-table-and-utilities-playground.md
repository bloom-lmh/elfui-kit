# 2026-07-19 虚拟表格、Utilities Playground 与视觉回归

## 主任务

- [x] Table：分析 10,000 行虚拟滚动热路径，降低快速滚动时的主线程占用。
- [x] Table：滚动更新合并到单帧，保持渲染行数有界，避免重复数据切片和无意义 DOM diff。
- [x] Table：补充快速跳转、连续滚动、稳定 key 与渲染窗口测试，并进行真实浏览器性能回归。
- [x] Utilities：把工具类集中到一个统一页面，不再使用零散静态目录式展示。
- [x] Utilities：实现参考 Vuetify 的交互式 Playground，包含分类/变体切换、实时预览、配置区、代码生成、复制和重置。
- [x] Utilities：每个工具类分类使用独立面板展示，并支持暗色模式与窄屏布局。

## 截图回归

- [x] Sticky：目标容器与 Teleport 案例的可见吸附层必须保持在目标容器边界内。
- [x] Splitter：垂直分割案例移除容器原生滚动条，同时保持分割条可拖动。
- [x] InputTag：保持标签输入控件语义和多标签布局，不能退化成普通 Input 外观。
- [x] Autocomplete：下拉选项和禁用项正确适配暗色模式。
- [x] Select：行内筛选案例避免控件过窄、标签与面板拥挤。
- [x] Transfer：长标签、Panel Footer 与窄容器案例必须显示面板内容和 footer。
- [x] ColorPicker：RGBA 状态文本必须完整显示右括号。
- [x] Cascader：只读验证下拉面板与输入框无缝贴合；并发线程负责其实现，本批不覆盖其工作区改动。

## 验收记录

- 10 个定向测试文件、131 项测试全部通过；生产站点构建和组件库构建均通过。
- 排除并发线程正在修改的 Cascader 测试后，全量 100 个测试文件、915 项测试全部通过；Cascader 当前仅剩清空插槽文本空白的严格断言由对应线程收口。
- 真实浏览器中 10,000 行表格快速合并 500 次滚动输入，仅派发 1 次帧级公开事件，DOM 保持 14–20 行。
- Utilities 在 1200px 与 700px 视口、light 与 dark token 下完成交互和无横向溢出检查。
- Sticky 吸附层实测位于目标容器顶部偏移 12px；Splitter 垂直拖拽由 48% 更新到 71%；浏览器控制台 0 error / 0 warning。

## 验收标准

1. 对受影响组件运行定向 Vitest，新增关键回归断言。
2. `pnpm build` 与 `pnpm build:lib` 通过。
3. 使用真实浏览器验证拖动、滚动、暗色主题、覆盖层定位与响应式布局。
4. 对应组件 `plan.md` 完成后打勾；未通过验收的项目不得提前标记完成。
