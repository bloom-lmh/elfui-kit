# 2026-07-21 文档导航、Playground 与导航组件细节修复

> 状态约定：`[ ]` 待处理，`[x]` 已完成并通过对应验证。

## A. 文档基础设施

- [x] 新增可复用的右侧页面目录组件：自动收集当前文档页的二、三级标题和 Playground 案例，支持点击定位、滚动激活、跨 Shadow Root 查找与窄屏隐藏。
- [x] 重构 Playground：右侧控制台可选、可折叠、占满预览区高度；案例水平垂直居中；预览与控制台不再出现重复边框；没有控制台时保持单栏布局。
- [x] 将本轮涉及的 Progress 与 Tabs 配置按钮迁移到 Playground 右侧控制台，标题栏仅保留当前值等交互状态。
- [x] 修复 Template/Script 代码规范化与高亮，源码不再被 token HTML 污染或截去开头。
- [ ] 完成其余全部历史页面的标题栏配置迁移（本轮目标组件已完成，旧页面继续按页面审计）。
- [ ] 完成全站全部历史案例标题、说明、状态和按钮的中英文迁移（本轮目标页面已完成，不能提前勾选全站项）。
- [ ] 完成全站全部需要外部数据、状态或方法案例的 Script 补齐（本轮目标页面已补齐，旧页面继续审计）。

## B. 表单与数据展示

- [x] Input 增补并验证 outlined 外观与文档案例。
- [x] Image 懒加载案例改为页面自然滚动触发，不再使用自建滚动面板。
- [x] 修复 VirtualList 快速拖动滚动条时闪白，使用同步窗口计算、稳定 DOM 回收、视口级 overscan 和边界钳制，并补充测试。
- [x] 修复 Carousel 外置指示器与卡片标签模式的灰色留白，图片与容器尺寸正确契合。
- [x] Carousel 全部目标案例补充中英文标题、图片替代文本、可复制模板与 Script 数据示例。
- [x] Progress 的条形、圆形、自动增长与速度设置迁入控制台，并补齐中英文文案和 Script。

## C. 导航组件

- [x] 重做 Anchor 水平模式的尺寸、滚动同步和激活指示，支持长目录；嵌套与横向案例补齐中英文和 Script。
- [x] Menu 案例移除多余外部卡片，交互状态放入标题区域，关键案例补齐中英文和 Script。
- [x] Tabs 修复铺满宽度，增加 start/center/end 对齐；重做可操作 Playground；默认使用非透明背景；图片切换增加过渡；标签变体拆为三个案例。
- [x] 参照 Vuetify linear stepper 交互语义重写 Steps：线性导航、可编辑跳转、内容面板、操作区、禁用边界与响应式布局一致。

## D. 验证

- [x] 生产文档构建通过。
- [x] 组件库产物构建与 TypeScript 声明生成通过。
- [x] 定向回归：13 个测试文件、149 项测试通过。
- [x] 全量回归：109 个测试文件、991 项测试通过。
- [x] 真实浏览器检查目录展示、英文切换、Carousel、Anchor、Menu、Tabs、Steps、Progress 控制台布局。
- [x] Playground 折叠、Anchor 定位和快速虚拟滚动由交互单测覆盖；浏览器自动化当前对 Custom Element 内部动态点击不回传状态，因此不将自动化点击结果作为唯一验收依据。
