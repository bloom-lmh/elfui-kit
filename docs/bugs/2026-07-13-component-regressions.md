# 组件案例回归修复清单（2026-07-13）

## 验收规则

- 每项缺陷必须同时检查组件实现、演示案例和定向测试。
- 所有演示标题、按钮和状态提示使用中文。
- 交互缺陷必须通过真实浏览器复现与回归，不能只依赖静态样式或单元测试。
- 修复完成后运行相关 Vitest、`pnpm build`，并记录全量测试的剩余失败。

## 表单与选择器

- [x] 01 InputTag：固定输入区域宽度；隐藏标签可发现、查看并删除，或支持内部水平浏览。
- [x] 02 Autocomplete：清除上一次选择造成的展示缓存，选项文字与实际选中值必须一致。
- [x] 03 Mention：第三个成员可选择；整理“整词触发/键盘选择”案例并说明用途。
- [x] 04 InputNumber：只读案例提供初始值；无控制按钮案例提供数值和说明。
- [x] 05 Textarea：自动高度案例随内容真实撑高。
- [x] 06 Switch：自定义值与内嵌提示恢复正常轨道尺寸和布局。
- [x] 07 CascaderPanel：切换任意父项时更新激活态和右侧子面板。
- [x] 08 Upload：分片上传按 `chunkSize` 真实拆分并展示正确分片数。
- [x] 09 Rate：移除 hover 模式；修复阈值颜色与图标案例的数值显示。
- [x] 10 Slider：范围与分段滑块不再产生重复 `[object Object]` key 警告。
- [x] 27 TimePicker：范围值可清空，上午/工作日/晚上快捷项可重复设置。

## 反馈组件

- [x] 11 Loading：消除 `defineExpose` 覆盖警告；全屏加载提供明确退出按钮。
- [x] 12 Message：文字、背景和边框适配暗色主题。
- [x] 13 Dialog：面板适配暗色主题。
- [x] 14 Drawer：面板与遮罩同步执行离场过渡。
- [x] 15 Tour：引导遮罩、目标高亮、步骤切换和关闭流程真实可用。

## 数据展示

- [x] 16 Result：重新设计视觉层级、图标、间距与操作区。
- [x] 17 Collapse：组合式 CollapseItem 展开/收起保持平滑，不坍缩闪动。
- [x] 18 InfiniteScroll：提供固定滚动视口和可见滚动条，触底可加载。
- [x] 19 Image：不同 object-fit 模式在案例中产生清晰可见差异。
- [x] 20 Pagination：点击页码后立即显示激活态，不依赖鼠标移出。
- [x] 21 Progress：条形进度支持配置增长动画速度。
- [x] 22 Carousel：统一 ghost 箭头与线形指示器；垂直案例使用正常可读宽度。
- [x] 26 Calendar：已选日期拥有明确、持久的高亮态。

## 导航与基础组件

- [x] 23 Menu：提供 toggle/search 插槽；内置 toggle 默认位于 header。
- [x] 24 Tabs：顶部导航案例拆分；card/position/editable 与 grow/stacked/color 状态隔离；优化操作按钮。
- [x] 25 Steps：点击步骤和上一步/下一步真实更新当前步骤。
- [x] 28 Link：`underline=false` 在默认、hover、focus 状态均无下划线。

## 验证记录

- 定向测试：29 个相关测试文件、235 个断言全部通过（覆盖表单、反馈、数据展示、导航与选择器）。
- 构建：`pnpm build` 通过（513 个模块）；仅保留 Vite 的大体积产物提示，非构建失败。
- 真实浏览器回归：验证 Autocomplete 的 Vue → React 重新筛选、Mention 第三个成员选择、CascaderPanel 父项切换、Loading 默认可操作与全屏入口；均无页面错误。修正后 `defineExpose` 不再覆盖宿主原生属性。
- 全量测试：本轮变更相关测试已全部通过；全量基线中历史遗留失败另行跟踪，不计入本缺陷清单。
